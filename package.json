{
  "name": "readonly-pop3-mcp",
  "version": "0.1.0",
  "description": "Strictly read-only POP3 MCP server for listing, reading, and searching email",
  "type": "module",
  "bin": {
    "readonly-pop3-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "SECURITY.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "mailparser": "^3.7.4",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.6",
    "@types/node": "^24.0.0",
    "typescript": "^6.0.0"
  },
  "license": "MIT"
}


